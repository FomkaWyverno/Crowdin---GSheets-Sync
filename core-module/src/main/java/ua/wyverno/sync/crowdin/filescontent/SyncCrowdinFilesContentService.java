package ua.wyverno.sync.crowdin.filescontent;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import ua.wyverno.sync.crowdin.sourcestrings.SyncCrowdinSourceStringsService;

@Service
public class SyncCrowdinFilesContentService {
    private final static Logger logger = LoggerFactory.getLogger(SyncCrowdinFilesContentService.class);

    public void synchronizationToFilesContent() {

    }
}
